---
phase: 03-documentation-changelog-agents
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .claude/commands/vit/execute-phase.md
  - files/commands/vit/execute-phase.md
  - .claude/vit/workflows/execute-phase.md
  - files/vit/workflows/execute-phase.md
autonomous: true

must_haves:
  truths:
    - "After execute-phase completes all waves and passes verification, vit-doc-updater spawns automatically without manual invocation"
    - "The doc-updater spawn passes PHASE_NUM, PHASE_NAME, PHASE_DIR, WORK_DIR, and MILESTONE as context"
    - "If the doc-updater fails or crashes, execute-phase logs one line and continues — it never aborts"
    - "vit-doc-updater appears in the model lookup table in execute-phase workflow"
    - "The doc-updater spawn happens after step 10 (commit phase completion) and before step 10.5 (push to GitHub)"
  artifacts:
    - path: ".claude/commands/vit/execute-phase.md"
      provides: "Doc-updater spawn step in execute-phase command"
      contains: "vit-doc-updater"
    - path: ".claude/vit/workflows/execute-phase.md"
      provides: "Doc-updater spawn step and model lookup entry in execute-phase workflow"
      contains: "vit-doc-updater"
  key_links:
    - from: ".claude/commands/vit/execute-phase.md"
      to: ".claude/agents/vit-doc-updater.md"
      via: "Task() call with subagent_type=vit-doc-updater"
      pattern: "vit-doc-updater"
    - from: ".claude/vit/workflows/execute-phase.md"
      to: "model lookup table"
      via: "vit-doc-updater row added to resolve_model_profile"
      pattern: "vit-doc-updater.*sonnet"
---

<objective>
Wire vit-doc-updater into execute-phase so it spawns automatically after all waves complete and verification passes.

Purpose: After execute-phase finishes all waves, commits phase completion (step 10), the doc-updater agent is spawned to update documentation and CHANGELOG. This happens before the push to GitHub (step 10.5) so doc-updater commits are included in the same push. Doc-updater failure is non-blocking.

Output: Modified execute-phase command and workflow (both `.claude/` and `files/` copies).
</objective>

<execution_context>
@./.claude/vit/workflows/execute-plan.md
@./.claude/vit/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-documentation-changelog-agents/03-RESEARCH.md

Prior plan context (agent definition from Plan 01):
- vit-doc-updater agent exists at .claude/agents/vit-doc-updater.md
- Agent expects PHASE_NUM, PHASE_NAME, PHASE_DIR, WORK_DIR, MILESTONE in its prompt context
- Agent reads SUMMARY.md/PLAN.md, updates docs sections, appends to CHANGELOG [Unreleased]
- Agent handles its own errors gracefully

Key files to modify:
@.claude/commands/vit/execute-phase.md
@files/commands/vit/execute-phase.md
@.claude/vit/workflows/execute-phase.md
@files/vit/workflows/execute-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add doc-updater spawn to execute-phase command</name>
  <files>
    .claude/commands/vit/execute-phase.md
    files/commands/vit/execute-phase.md
  </files>
  <action>
  In the execute-phase command (`.claude/commands/vit/execute-phase.md` and `files/commands/vit/execute-phase.md`), add a step **10.6** between step 10 (Commit phase completion) and step 10.5 (Push to GitHub).

  Step 10.6 spawns the doc-updater AFTER phase completion is committed, so SUMMARY.md files are available. It runs BEFORE the push so doc-updater commits are included in the same push.

  Add this step:

  ```
  10.6. **Spawn doc-updater** (after phase completion commit)

     Spawn vit-doc-updater to update documentation files and CHANGELOG:

     ```
     Task(
       prompt="""
     <context>
     Phase Number: {PHASE_NUM}
     Phase Name: {PHASE_NAME}
     Phase Directory: {PHASE_DIR}
     Working Directory: {WORK_DIR}
     Milestone: {MILESTONE}
     </context>

     Read all SUMMARY.md and PLAN.md files in the phase directory.
     Determine which documentation sections need updating.
     Update only those sections in README.md and docs/ files.
     Append a new entry to the [Unreleased] section of CHANGELOG.md.
     Commit documentation changes.
     """,
       subagent_type="vit-doc-updater",
       model="{doc_updater_model}",
       description="Update docs for Phase {PHASE_NUM}"
     ) || log "[doc-updater failed — continuing]"
     ```

     The `{doc_updater_model}` comes from the model lookup table (resolved in the workflow's resolve_model_profile step). In the command file, reference it as the model resolved for vit-doc-updater.

     If the Task call fails or the agent crashes: log `[doc-updater failed — continuing]` and proceed to step 10.5 (push). The doc-updater is non-blocking — it must NEVER prevent execute-phase from completing.
  ```

  **Placement:** After step 10 (Commit phase completion), before step 10.5 (Push to GitHub). The step ordering in the file should read: 10, 10.6 (new), 10.5, 11.

  Note: Yes, 10.6 comes before 10.5 numerically — this is because 10.5 was already numbered when this insertion was designed. The doc-updater must run BEFORE the push so its commits are included. The executor should place this step physically between steps 10 and 10.5 in the file, regardless of the numbering oddity.

  **Error handling:** The `|| log` pattern after the Task call ensures doc-updater crashes don't abort execute-phase. This matches the non-blocking pattern used for PR promotion and PR reviewer.

  IMPORTANT: Both `.claude/` and `files/` copies must be exactly identical after edits.
  </action>
  <verify>
  Run:
  ```bash
  grep -c "vit-doc-updater" .claude/commands/vit/execute-phase.md
  grep -c "doc-updater failed" .claude/commands/vit/execute-phase.md
  grep -c "10.6" .claude/commands/vit/execute-phase.md
  diff .claude/commands/vit/execute-phase.md files/commands/vit/execute-phase.md
  ```
  First grep >= 1 (agent referenced). "doc-updater failed" >= 1 (error handling). "10.6" >= 1 (step number). Diff returns empty (files identical).
  </verify>
  <done>execute-phase command spawns vit-doc-updater after phase completion commit (step 10) and before push (step 10.5), with non-blocking error handling. Both .claude/ and files/ copies are identical.</done>
</task>

<task type="auto">
  <name>Task 2: Add doc-updater spawn and model entry to execute-phase workflow</name>
  <files>
    .claude/vit/workflows/execute-phase.md
    files/vit/workflows/execute-phase.md
  </files>
  <action>
  In the execute-phase workflow (`.claude/vit/workflows/execute-phase.md` and `files/vit/workflows/execute-phase.md`), make two changes:

  **Change 1: Add vit-doc-updater to the model lookup table.**

  In the `<step name="resolve_model_profile">` step, there is a table:

  ```
  | Agent | quality | balanced | budget |
  |-------|---------|----------|--------|
  | vit-executor | opus | sonnet | sonnet |
  | vit-verifier | sonnet | sonnet | haiku |
  | general-purpose | — | — | — |
  ```

  Add a new row:
  ```
  | vit-doc-updater | sonnet | sonnet | haiku |
  ```

  This gives the doc-updater the same model profile as vit-verifier (content analysis and generation, not creative work).

  **Change 2: Add a `<step name="spawn_doc_updater">` between `update_roadmap` and `offer_next` steps.**

  Insert a new step:

  ```xml
  <step name="spawn_doc_updater">
  **Spawn doc-updater** (after phase completion commit, before push):

  Spawn vit-doc-updater to update documentation files and CHANGELOG.

  ```
  Task(
    prompt="""
  <context>
  Phase Number: {PHASE_NUM}
  Phase Name: {PHASE_NAME}
  Phase Directory: {PHASE_DIR}
  Working Directory: {WORK_DIR}
  Milestone: {MILESTONE}
  </context>

  Read all SUMMARY.md and PLAN.md files in the phase directory.
  Determine which documentation sections need updating.
  Update only those sections in README.md and docs/ files.
  Append a new entry to the [Unreleased] section of CHANGELOG.md.
  Commit documentation changes.
  """,
    subagent_type="vit-doc-updater",
    model="{doc_updater_model}",
    description="Update docs for Phase {PHASE_NUM}"
  ) || log "[doc-updater failed — continuing]"
  ```

  Non-blocking: if Task fails, log one line and continue to offer_next.
  </step>
  ```

  Keep the step concise — the workflow is a reference, not the primary prompt. Focus on: Task() call with context variables, non-blocking error handling.

  IMPORTANT: Both `.claude/` and `files/` copies must be exactly identical after edits.
  </action>
  <verify>
  Run:
  ```bash
  grep -c "vit-doc-updater" .claude/vit/workflows/execute-phase.md
  grep -c "spawn_doc_updater" .claude/vit/workflows/execute-phase.md
  grep -c "doc-updater failed" .claude/vit/workflows/execute-phase.md
  # Model table has the new row
  grep "vit-doc-updater.*sonnet.*sonnet.*haiku" .claude/vit/workflows/execute-phase.md
  diff .claude/vit/workflows/execute-phase.md files/vit/workflows/execute-phase.md
  ```
  vit-doc-updater >= 2 (model table + spawn step). spawn_doc_updater >= 1. "doc-updater failed" >= 1. Model table grep matches. Diff returns empty.
  </verify>
  <done>execute-phase workflow has vit-doc-updater in the model lookup table (sonnet/sonnet/haiku) and a spawn_doc_updater step after update_roadmap, with non-blocking error handling. Both .claude/ and files/ copies are identical.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. execute-phase command has step 10.6 spawning vit-doc-updater after step 10 (commit)
2. execute-phase workflow has spawn_doc_updater step after update_roadmap
3. Doc-updater spawn passes PHASE_NUM, PHASE_NAME, PHASE_DIR, WORK_DIR, MILESTONE as context
4. vit-doc-updater appears in the model lookup table as sonnet/sonnet/haiku
5. Doc-updater failure logs one line and does not abort execute-phase
6. All modified files have identical .claude/ and files/ copies
</verification>

<success_criteria>
- vit-doc-updater spawns automatically after phase completion commit
- Spawn happens before push to GitHub (doc commits included in push)
- Task() call provides all required context (PHASE_NUM, PHASE_NAME, PHASE_DIR, WORK_DIR, MILESTONE)
- Model lookup table includes vit-doc-updater (sonnet/sonnet/haiku)
- Doc-updater failure is non-blocking (log one line, continue)
- All .claude/ and files/ copies are identical
</success_criteria>

<output>
After completion, create `.planning/phases/03-documentation-changelog-agents/03-03-SUMMARY.md`
</output>
