---
phase: 03-docs-gap-closure
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .github/workflows/deploy-docs.yml
autonomous: true
assigned_to: ""
execute_by: claude
gap_closure: true

must_haves:
  truths:
    - "VitePress builds without errors after all content fixes from Plan 01"
    - "deploy-docs.yml uses a valid, resolvable GitHub Action for setup-node"
    - "All 10+ content pages exist as HTML files in docs/.vitepress/dist/"
    - "Phase 02 has a formal VERIFICATION.md documenting build status"
  artifacts:
    - path: ".github/workflows/deploy-docs.yml"
      provides: "Valid GitHub Pages deployment workflow"
      contains: "actions/setup-node@v"
    - path: ".planning/phases/v1.1/02-docs-site/02-05-SUMMARY.md"
      provides: "Plan 05 completion record"
      contains: "build exits 0"
    - path: ".planning/phases/v1.1/02-docs-site/02-VERIFICATION.md"
      provides: "Phase 02 formal verification"
      contains: "PASSED"
  key_links:
    - from: ".github/workflows/deploy-docs.yml"
      to: "docs/.vitepress/dist"
      via: "npm run build + upload-pages-artifact"
      pattern: "npm run build"
---

<objective>
Investigate the CI action version issue, run full build verification, and produce the missing Phase 02 verification artifacts.

Purpose: Close audit gaps 1 and 2 (critical) — confirm the deployment workflow is valid and produce the formal verification that Phase 02 was skipped. This unblocks the milestone.
Output: Verified deployment workflow, build verification results, VERIFICATION.md and Plan 05 SUMMARY.
</objective>

<execution_context>
@./.claude/vit/workflows/execute-plan.md
@./.claude/vit/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/v1.1/03-docs-gap-closure/03-RESEARCH.md
@.planning/phases/v1.1/03-docs-gap-closure/03-01-SUMMARY.md
@.planning/phases/v1.1/02-docs-site/02-05-PLAN.md
@.planning/phases/v1.1/02-docs-site/02-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Investigate CI action version and fix if needed</name>
  <files>.github/workflows/deploy-docs.yml</files>
  <action>
The audit claims `actions/setup-node@v6` "does not exist" and blocks deployment. Research confirms v6 IS valid (v6.3.0 released March 2026). Investigate the real situation:

1. **Check if the action resolves:** Run `gh api repos/actions/setup-node/git/ref/tags/v6 --jq '.ref' 2>&1` to confirm the tag exists on GitHub. If it returns successfully, v6 is valid.

2. **Check CI run history:** Run `gh run list --workflow=deploy-docs.yml --limit 5 2>&1` to see if any runs exist and their status. If runs exist, check the failing one: `gh run view <run-id> --log-failed 2>&1`.

3. **Decision tree:**
   - If v6 tag resolves AND CI has never run (workflow only triggers on push to main): Leave `actions/setup-node@v6` as-is. The workflow is valid; it just hasn't been triggered yet because docs haven't been merged to main.
   - If v6 tag resolves BUT CI fails on this action: Check the error message and fix accordingly.
   - If v6 tag does NOT resolve (unlikely per research): Change to `actions/setup-node@v4` as the audit suggests.

4. **Add a YAML comment** above the setup-node step noting the version was verified: `# Verified: actions/setup-node@v6 exists (v6.3.0, March 2026)`

Only modify the workflow file if there is an actual issue to fix. Do NOT downgrade blindly.
  </action>
  <verify>
- `grep "actions/setup-node@v" .github/workflows/deploy-docs.yml` — shows the action version in use
- `gh api repos/actions/setup-node/git/ref/tags/v6 --jq '.ref' 2>&1` — confirms tag exists (or document why it doesn't)
  </verify>
  <done>The deploy-docs.yml workflow uses a verified, valid action version. If v6 was valid, it stays; if not, it was changed to a working version with documented reasoning.</done>
</task>

<task type="auto">
  <name>Task 2: Run build verification and produce Phase 02 verification artifacts</name>
  <files></files>
  <action>
This task completes the work that Plan 05 of Phase 02 was supposed to do: automated build verification and formal verification artifacts.

1. **Run full build:** Execute `cd docs && npm run build 2>&1` and capture the complete output.

2. **Count HTML pages:** Run `find docs/.vitepress/dist -name "*.html" | wc -l`. Expect at least 12 (index + 10 content pages + category index pages).

3. **Check for dead links:** Scan the build output for any "dead link" or "404" warnings. VitePress reports these during build.

4. **Check page content size:** For each HTML file in dist/, verify it has substantive content (not an empty stub). A quick `wc -c` on each file should show >1000 bytes.

5. **Produce 02-05-SUMMARY.md:** Write `.planning/phases/v1.1/02-docs-site/02-05-SUMMARY.md` documenting:
   - Build result (exit code, warnings)
   - HTML page count
   - Dead link status
   - Note that UAT passed 8/8 (from 02-UAT.md) satisfying the human checkpoint
   - Use the standard summary template format

6. **Produce 02-VERIFICATION.md:** Write `.planning/phases/v1.1/02-docs-site/02-VERIFICATION.md` documenting:
   - Phase 02 truths verified (from 02-05-PLAN.md must_haves):
     a. VitePress builds without errors
     b. All internal links resolve (no dead links)
     c. All 10 content pages have substantive content
     d. Sidebar navigation matches hierarchy
   - For each truth: status (PASSED/FAILED), evidence (build output, file counts, UAT reference)
   - Overall status: PASSED or FAILED
   - Reference UAT results (8/8 passed) as evidence for visual/content verification

The VERIFICATION.md should follow the pattern of existing verification files in the project.
  </action>
  <verify>
- `cd docs && npm run build 2>&1` exits 0
- `find docs/.vitepress/dist -name "*.html" | wc -l` shows >= 12
- `test -f .planning/phases/v1.1/02-docs-site/02-05-SUMMARY.md && echo "EXISTS"` — file exists
- `test -f .planning/phases/v1.1/02-docs-site/02-VERIFICATION.md && echo "EXISTS"` — file exists
- `grep "PASSED" .planning/phases/v1.1/02-docs-site/02-VERIFICATION.md` — shows verification passed
  </verify>
  <done>VitePress build succeeds with zero warnings. Phase 02 has formal VERIFICATION.md (all truths passed) and Plan 05 SUMMARY.md documenting build results and UAT sign-off.</done>
</task>

</tasks>

<verification>
1. deploy-docs.yml uses a verified, valid action version
2. VitePress build exits 0 with no dead link warnings
3. At least 12 HTML files in dist/
4. 02-05-SUMMARY.md exists and documents build results
5. 02-VERIFICATION.md exists with overall PASSED status
</verification>

<success_criteria>
- deploy-docs.yml action version is verified as valid (v6 confirmed or changed to working version)
- VitePress build exits 0 with zero warnings after Plan 01 content fixes
- 02-05-SUMMARY.md and 02-VERIFICATION.md exist with documented evidence
- All Phase 02 must_have truths are documented as PASSED in VERIFICATION.md
- Audit gaps 1 and 2 are fully closed
</success_criteria>

<output>
After completion, create `.planning/phases/v1.1/03-docs-gap-closure/03-02-SUMMARY.md`
</output>
