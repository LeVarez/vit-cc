---
phase: 03-docs-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/reference/hooks-sessions.md
  - docs/reference/configuration.md
  - docs/guide/how-vit-works.md
  - docs/guide/architecture.md
  - docs/contributing/agent-anatomy.md
  - README.md
autonomous: true
assigned_to: ""
execute_by: claude
gap_closure: true

must_haves:
  truths:
    - "hooks-sessions.md says 'npx vit-claude' not 'npm install vit-cc'"
    - "README docs link points to https://levarez.github.io/vit-cc/ not vit-claude.dev"
    - "Model profiles matrix in configuration.md uses short form (opus, sonnet, haiku) matching source of truth"
    - "templates-references.md and hooks-sessions.md each have at least one incoming cross-link from another docs page"
  artifacts:
    - path: "docs/reference/hooks-sessions.md"
      provides: "Corrected install command"
      contains: "npx vit-claude"
    - path: "docs/reference/configuration.md"
      provides: "Corrected model naming"
      contains: "| vit-planner | opus |"
    - path: "README.md"
      provides: "Corrected docs URL"
      contains: "levarez.github.io/vit-cc"
  key_links:
    - from: "docs/guide/how-vit-works.md"
      to: "/reference/hooks-sessions"
      via: "inline markdown link"
      pattern: "\\(/reference/hooks-sessions\\)"
    - from: "docs/guide/architecture.md"
      to: "/guide/templates-references"
      via: "inline markdown link"
      pattern: "\\(/guide/templates-references\\)"
    - from: "docs/contributing/agent-anatomy.md"
      to: "/reference/hooks-sessions"
      via: "inline markdown link"
      pattern: "\\(/reference/hooks-sessions\\)"
---

<objective>
Fix all content inconsistencies and add missing cross-links identified in the v1.1 milestone audit.

Purpose: Close audit gaps 3-6 (non-critical content issues) so the docs site is accurate and well-linked before final build verification.
Output: Six corrected documentation files with consistent content and improved internal linking.
</objective>

<execution_context>
@./.claude/vit/workflows/execute-plan.md
@./.claude/vit/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/v1.1/03-docs-gap-closure/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix content inconsistencies (audit gaps 3, 4, 5)</name>
  <files>
    docs/reference/hooks-sessions.md
    docs/reference/configuration.md
    README.md
  </files>
  <action>
Three targeted edits:

1. **hooks-sessions.md line 11** — Change `npm install vit-cc` to `npx vit-claude`. This is the install command used everywhere else in the project.

2. **README.md line 619** — Change `https://vit-claude.dev` to `https://levarez.github.io/vit-cc/`. The placeholder domain does not exist; the actual docs site deploys to GitHub Pages.

3. **configuration.md model profiles matrix (around lines 57-67)** — Change the `claude-opus`, `claude-sonnet`, `claude-haiku` format to the short form `opus`, `sonnet`, `haiku`. The source of truth is `files/vit/references/model-profiles.md` which uses short form. The README and architecture.md already use short form. Only configuration.md has the `claude-` prefix — fix it to match.

The matrix should look like:
```
| vit-planner | opus | opus | sonnet |
| vit-roadmapper | opus | sonnet | sonnet |
...etc matching files/vit/references/model-profiles.md exactly
```

Do NOT change any other content in these files. These are surgical single-line or single-table edits.
  </action>
  <verify>
Run these checks:
- `grep "npx vit-claude" docs/reference/hooks-sessions.md` — should match line 11
- `grep "npm install vit-cc" docs/reference/hooks-sessions.md` — should return NO matches
- `grep "levarez.github.io/vit-cc" README.md` — should match
- `grep "vit-claude.dev" README.md` — should return NO matches
- `grep "claude-opus\|claude-sonnet\|claude-haiku" docs/reference/configuration.md` — should return NO matches
- `grep "| vit-planner | opus |" docs/reference/configuration.md` — should match
  </verify>
  <done>All three content inconsistencies are fixed: install command corrected, README URL updated, model naming uses short form consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Add cross-links to isolated pages (audit gap 6)</name>
  <files>
    docs/guide/how-vit-works.md
    docs/guide/architecture.md
    docs/contributing/agent-anatomy.md
  </files>
  <action>
Add contextually appropriate incoming links to `templates-references.md` and `hooks-sessions.md`, which currently have zero incoming cross-links (only reachable via sidebar).

Research identified these natural link points:

1. **docs/guide/how-vit-works.md** — This page covers session continuity and the VIT execution loop. Find the section discussing session management, pause/resume, or hook behavior. Add an inline link to `/reference/hooks-sessions` where session management is mentioned. Example: "VIT uses [hook files and session management](/reference/hooks-sessions) to maintain continuity across context windows."

2. **docs/guide/architecture.md** — This page covers the overall VIT architecture including templates and references. Find where it mentions templates, reference files, or the files/ directory structure. Add an inline link to `/guide/templates-references`. Example: "These are documented in the [Templates and References](/guide/templates-references) guide."

3. **docs/contributing/agent-anatomy.md** — This page covers how agents work, including their hook files. Find where hooks are mentioned. Add an inline link to `/reference/hooks-sessions`. Example: "For details on hook file installation and session management, see [Hooks and Sessions](/reference/hooks-sessions)."

IMPORTANT: Do NOT add links that feel forced. Each link must fit naturally into an existing paragraph or sentence. If a natural placement doesn't exist in one of these files, skip that link and add it to a different file where it fits better. The goal is at least one incoming link to each of the two isolated pages.
  </action>
  <verify>
Run these checks:
- `grep -c "/reference/hooks-sessions" docs/guide/how-vit-works.md docs/contributing/agent-anatomy.md` — at least 1 match in one of these files
- `grep -c "/guide/templates-references" docs/guide/architecture.md` — at least 1 match
- `cd docs && npm run build 2>&1 | grep -i "dead link\|404"` — no dead link warnings (confirms links are valid)
  </verify>
  <done>Both templates-references.md and hooks-sessions.md have at least one incoming cross-link from another content page. Links are contextually natural, not forced.</done>
</task>

</tasks>

<verification>
1. All six grep checks from Task 1 pass
2. Cross-link grep checks from Task 2 pass
3. VitePress build still succeeds with no dead link warnings: `cd docs && npm run build 2>&1`
</verification>

<success_criteria>
- Zero instances of "npm install vit-cc" in hooks-sessions.md
- Zero instances of "vit-claude.dev" in README.md
- Zero instances of "claude-opus" or "claude-sonnet" or "claude-haiku" in configuration.md
- At least 1 incoming link to hooks-sessions.md from another page
- At least 1 incoming link to templates-references.md from another page
- VitePress build exits 0 with no warnings
</success_criteria>

<output>
After completion, create `.planning/phases/v1.1/03-docs-gap-closure/03-01-SUMMARY.md`
</output>
