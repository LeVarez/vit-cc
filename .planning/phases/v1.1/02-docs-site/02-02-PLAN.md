---
phase: 02-docs-site
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - docs/guide/how-vit-works.md
  - docs/guide/architecture.md
  - docs/guide/workflow-files.md
  - docs/guide/templates-references.md
autonomous: true
assigned_to: ""
execute_by: claude

must_haves:
  truths:
    - "A reader understands the VIT core loop (plan, execute, verify) after reading How VIT Works"
    - "A reader understands orchestrator patterns, wave parallelization, and goal-backward verification after reading Architecture"
    - "A reader knows what workflow files are, where they live, and how they are referenced after reading Workflow Files"
    - "A reader knows the difference between templates and references and where each lives after reading Templates and References"
  artifacts:
    - path: "docs/guide/how-vit-works.md"
      provides: "Core loop explanation, state management, context resilience"
      min_lines: 100
    - path: "docs/guide/architecture.md"
      provides: "Orchestrator pattern, wave parallelization, goal-backward verification, state flow diagram"
      min_lines: 120
    - path: "docs/guide/workflow-files.md"
      provides: "Workflow purpose, location, @-referencing, step naming"
      min_lines: 60
    - path: "docs/guide/templates-references.md"
      provides: "Template vs reference distinction, locations, consumption patterns"
      min_lines: 60
  key_links:
    - from: "docs/guide/how-vit-works.md"
      to: "docs/guide/architecture.md"
      via: "cross-link for deeper dive"
      pattern: "\\[Architecture.*\\]\\(/guide/architecture\\)"
    - from: "docs/guide/architecture.md"
      to: "docs/guide/workflow-files.md"
      via: "cross-link for workflow details"
      pattern: "\\[Workflow.*\\]\\(/guide/workflow-files\\)"
---

<objective>
Write the 4 Guide section pages with substantive content derived from the existing codebase source files.

Purpose: The Guide section is the "understand VIT" path. A new contributor reads these to grasp how VIT works before trying to extend it.
Output: 4 complete documentation pages covering DOCS-03, DOCS-04, DOCS-07, DOCS-08.
</objective>

<execution_context>
@./.claude/vit/workflows/execute-plan.md
@./.claude/vit/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/v1.1/02-docs-site/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write How VIT Works and Architecture Deep Dive pages</name>
  <files>
    docs/guide/how-vit-works.md
    docs/guide/architecture.md
  </files>
  <action>
**How VIT Works (DOCS-03) — `docs/guide/how-vit-works.md`:**

Read these source files for content:
- `files/vit/workflows/execute-phase.md` (core execution loop)
- `files/vit/workflows/execute-plan.md` (plan execution details)
- `files/vit/references/continuation-format.md` (context resilience)
- `files/vit/templates/state.md` (state management)

Write a guide covering:
1. **The Core Loop** — new-project -> plan-phase -> execute-phase -> verify-work cycle. Explain each step with what it does, what it produces, and what triggers the next step.
2. **State Management** — How `.planning/` directory serves as project memory. STATE.md tracks position, decisions, context. ROADMAP.md tracks phases. SUMMARY.md files record what was built.
3. **Context Resilience** — Why context windows are a constraint. How VIT handles it: continuation files (`.continue-here`), state serialization, summary-based history loading. The 50% context budget rule.
4. **The .planning/ Directory** — Directory structure walkthrough: PROJECT.md, ROADMAP.md, STATE.md, REQUIREMENTS.md, phases/ with PLAN.md, SUMMARY.md, VERIFICATION.md.

Include a Mermaid diagram showing the core loop flow. Cross-link to Architecture Deep Dive for implementation details.

**Architecture Deep Dive (DOCS-04) — `docs/guide/architecture.md`:**

Read these source files for content:
- `files/commands/vit/execute-phase.md` (orchestrator pattern)
- `files/vit/workflows/execute-phase.md` (wave execution)
- `files/agents/vit-planner.md` (goal-backward verification section)
- `files/vit/references/model-profiles.md` (model profile system)
- `files/vit/workflows/verify-phase.md` (verification)

Write a guide covering:
1. **Orchestrator Pattern** — Commands are orchestrators that spawn agents via Task(). Commands define WHAT to do, agents define HOW. Show the execute-phase command as example: it reads plans, groups by wave, spawns vit-executor for each plan.
2. **Wave Parallelization** — How plans are grouped into waves via `wave` frontmatter. Wave 1 plans run in parallel, Wave 2 waits for Wave 1, etc. Show a dependency graph example.
3. **Goal-Backward Verification** — The must_haves system: truths (observable behaviors), artifacts (files that must exist), key_links (critical connections). How the verifier checks these after execution.
4. **State Flow Diagram** — Mermaid diagram showing data flow: ROADMAP -> PLAN.md -> executor -> SUMMARY.md -> verifier -> VERIFICATION.md -> STATE.md update.
5. **Model Profile System** — Three profiles (quick, balanced, comprehensive) that map agents to different Claude models. How `set-profile` changes the active profile. Show the profile matrix.

Cross-link to Workflow Files guide for workflow implementation details.
  </action>
  <verify>
Run `cd docs && npm run build 2>&1 | grep -i 'dead link\|error'` — must return empty (no errors). Verify both files have >100 lines: `wc -l docs/guide/how-vit-works.md docs/guide/architecture.md`. Verify Mermaid diagrams exist: `grep -c 'mermaid' docs/guide/how-vit-works.md docs/guide/architecture.md`.
  </verify>
  <done>How VIT Works page explains core loop, state management, context resilience, and .planning/ structure with Mermaid diagram. Architecture page covers orchestrator pattern, wave parallelization, goal-backward verification, state flow, and model profiles with Mermaid diagram. Both pages have substantive content (not stubs).</done>
</task>

<task type="auto">
  <name>Task 2: Write Workflow Files and Templates/References pages</name>
  <files>
    docs/guide/workflow-files.md
    docs/guide/templates-references.md
  </files>
  <action>
**Workflow Files (DOCS-07) — `docs/guide/workflow-files.md`:**

Read the `files/vit/workflows/` directory — scan all workflow files to understand patterns.

Write a guide covering:
1. **Purpose** — Workflows define step-by-step procedures that agents follow. They are the "how-to" for each operation. They are referenced via `@` in agent prompts and plan execution contexts.
2. **Location** — All workflows live in `files/vit/workflows/`. List all workflow files with a one-line description of each.
3. **@-Referencing** — How workflows are included in execution context via `@./.claude/vit/workflows/execute-plan.md` syntax. Claude Code resolves these at prompt assembly time.
4. **Step Naming** — Workflows use `<step name="...">` XML tags to define discrete operations. Agents can reference specific steps.
5. **Example Structure** — Show a simplified example of a workflow file structure (use execute-plan.md as the example), explaining each section.

**Templates and References (DOCS-08) — `docs/guide/templates-references.md`:**

Read `files/vit/templates/` and `files/vit/references/` directories.

Write a guide covering:
1. **Templates vs References** — Templates are document skeletons that get filled in (e.g., state.md template -> actual STATE.md). References are static knowledge bases that agents consult but don't modify (e.g., ui-brand.md, model-profiles.md).
2. **Template Location** — `files/vit/templates/`. List key templates with their purpose.
3. **Reference Location** — `files/vit/references/`. List all references with their purpose.
4. **Consumption Pattern** — Agents reference templates when creating new documents. Agents reference references when making decisions. Show examples of each pattern.
5. **ui-brand.md** — Special callout for the UI brand reference — defines ASCII art patterns, checkpoint box dimensions, status symbols. Used by any agent that produces terminal output.
  </action>
  <verify>
Run `cd docs && npm run build 2>&1 | grep -i 'dead link\|error'` — must return empty. Verify both files have >60 lines: `wc -l docs/guide/workflow-files.md docs/guide/templates-references.md`.
  </verify>
  <done>Workflow Files page documents purpose, location, @-referencing, step naming, and example structure. Templates and References page distinguishes templates from references, lists key files, explains consumption patterns, and highlights ui-brand.md.</done>
</task>

</tasks>

<verification>
1. `cd docs && npm run build` exits 0 with no dead link warnings
2. All 4 guide pages have substantive content (>60 lines each)
3. Mermaid diagrams present in how-vit-works.md and architecture.md
4. Cross-links between guide pages use correct VitePress link format (`/guide/...`)
5. Content is derived from actual source files, not generic placeholder text
</verification>

<success_criteria>
- All 4 guide pages have substantive, accurate content derived from codebase source files
- A reader can understand VIT's core loop, architecture, workflows, and template system
- Mermaid diagrams render (verified by build success with mermaid plugin)
- Internal cross-links resolve correctly
- VitePress builds without errors
</success_criteria>

<output>
After completion, create `.planning/phases/v1.1/02-docs-site/02-02-SUMMARY.md`
</output>
