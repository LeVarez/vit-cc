---
phase: 02-docs-site
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified: []
autonomous: false
assigned_to: ""
execute_by: claude
user_setup:
  - service: github-pages
    why: "GitHub Pages deployment requires manual settings change"
    dashboard_config:
      - task: "Set Pages source to GitHub Actions"
        location: "GitHub repo Settings -> Pages -> Build and deployment -> Source -> select 'GitHub Actions'"

must_haves:
  truths:
    - "VitePress builds without errors after all content pages are complete"
    - "All internal links resolve correctly (no dead links in build output)"
    - "All 10 content pages have substantive content (not stubs)"
    - "Sidebar navigation matches Guide -> Contributing -> Reference hierarchy"
  artifacts: []
  key_links: []
---

<objective>
Final build verification and human checkpoint to confirm the docs site looks correct.

Purpose: Validates the complete docs site after all content plans have executed. Catches any broken links, build errors, or visual issues before deployment.
Output: Verified, buildable docs site ready for GitHub Pages deployment.
</objective>

<execution_context>
@./.claude/vit/workflows/execute-plan.md
@./.claude/vit/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build verification and fix any issues</name>
  <files></files>
  <action>
1. Run `cd docs && npm run build` and capture full output.
2. Check for any dead link warnings — if found, fix the broken links in the source markdown files.
3. Check for any build errors — if found, diagnose and fix.
4. Verify all 10 content pages (not counting index.md) have >50 lines of content (not stubs).
5. Start the VitePress preview server: `cd docs && npm run preview` (in background).
6. Verify the site is accessible at the preview URL.

If any issues are found, fix them. The goal is a clean build with zero warnings.
  </action>
  <verify>
`cd docs && npm run build 2>&1` exits 0 with no warnings. `find docs/.vitepress/dist -name "*.html" | wc -l` shows at least 11 HTML files (index + 10 content pages).
  </verify>
  <done>VitePress builds cleanly with zero errors and zero warnings. All 11 pages render as HTML in dist/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete VitePress documentation site with landing page, 4 guide pages, 3 contributing pages, and 3 reference pages. Brand-themed with blue-violet colors, Mermaid diagrams, and full sidebar navigation.</what-built>
  <how-to-verify>
1. Run `cd docs && npx vitepress preview --port 4173` to start the preview server
2. Open http://localhost:4173/vit-cc/ in your browser
3. Verify the landing page: hero section with VIT branding, "Get Started" and "GitHub" buttons, 4-feature grid
4. Click through each sidebar section:
   - Guide: How VIT Works, Architecture Deep Dive, Workflow Files, Templates and References
   - Contributing: Command Anatomy, Agent Anatomy, Testing
   - Reference: GitHub Integration, Configuration, Hooks and Sessions
5. Verify content is substantive (not placeholder text) on each page
6. Verify Mermaid diagrams render on Architecture and GitHub Integration pages
7. Verify brand colors (blue-violet theme) are applied
8. Check that internal links work when clicked
9. Test dark mode toggle
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. VitePress build exits 0 with no errors or dead link warnings
2. All 11 pages (index + 10 content) exist as HTML in dist/
3. Human confirms visual quality and content completeness
</verification>

<success_criteria>
- Clean VitePress build with zero warnings
- Human has verified all pages render correctly with substantive content
- Site is ready for GitHub Pages deployment (deploy-docs.yml workflow will handle actual deployment on merge to main)
</success_criteria>

<output>
After completion, create `.planning/phases/v1.1/02-docs-site/02-05-SUMMARY.md`
</output>
