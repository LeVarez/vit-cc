---
phase: 01-infrastructure-and-ia
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - docs/.vitepress/sidebars.ts
  - docs/.vitepress/config.mts
  - docs/index.md
  - docs/getting-started/index.md
  - docs/getting-started/installation.md
  - docs/getting-started/first-project.md
  - docs/concepts/index.md
  - docs/concepts/phases.md
  - docs/concepts/milestones.md
  - docs/concepts/agents.md
  - docs/concepts/state.md
  - docs/concepts/workflow-loop.md
  - docs/commands/index.md
  - docs/agents/index.md
  - docs/guides/index.md
  - docs/guides/build-a-project.md
  - docs/guides/add-a-milestone.md
  - docs/guides/debug-with-vit.md
  - docs/guides/custom-agents.md
  - docs/internals/index.md
  - docs/internals/state-management.md
  - docs/internals/github-sync.md
  - docs/internals/worktrees.md
  - docs/internals/hooks.md
  - docs/config/index.md
  - docs/config/profiles.md
  - docs/config/settings.md
  - docs/config/hooks.md
autonomous: true
assigned_to: ""
execute_by: claude

must_haves:
  truths:
    - "Every sidebar section is visible in the navigation and all links resolve to existing pages"
    - "The homepage displays a hero section with value proposition, a quick-start CTA, and feature highlights"
    - "All 7 content sections appear in the sidebar when navigating to their respective directories"
    - "Dark mode toggle works and content is readable in both modes"
    - "The search bar returns results for placeholder page titles"
  artifacts:
    - path: "docs/.vitepress/sidebars.ts"
      provides: "Complete sidebar configuration for all 7 sections"
      exports: ["gettingStartedSidebar", "conceptsSidebar", "commandsSidebar", "agentsSidebar", "guidesSidebar", "internalsSidebar", "configSidebar"]
    - path: "docs/index.md"
      provides: "Homepage with hero section, features, and CTA"
      contains: "layout: home"
    - path: "docs/getting-started/index.md"
      provides: "Getting started section landing page"
    - path: "docs/commands/index.md"
      provides: "Commands section landing page with placeholder for categorized table"
    - path: "docs/agents/index.md"
      provides: "Agents section landing page with placeholder for grouped table"
  key_links:
    - from: "docs/.vitepress/sidebars.ts"
      to: "docs/.vitepress/config.mts"
      via: "import statement in config.mts"
      pattern: "import.*from.*sidebars"
    - from: "docs/.vitepress/config.mts"
      to: "docs/**/*.md"
      via: "sidebar link paths matching file-based routes"
      pattern: "link:.*/"
    - from: "docs/index.md"
      to: "docs/getting-started/"
      via: "hero action button CTA"
      pattern: "link:.*getting-started"
---

<objective>
Create the homepage, define the complete sidebar structure, and generate placeholder pages for every section so the site is fully navigable.

Purpose: The sidebar structure IS the information architecture. Committing it before any content is written prevents the costly restructuring of 62 cross-linked pages later. Every sidebar link must resolve to a real page — no dead links, no invisible content.
Output: A homepage with hero/features/CTA, a sidebars.ts with all 7 sections fully populated, and placeholder .md files for every non-command/non-agent page (command and agent pages are stub-extracted in Phases 03-04).
</objective>

<execution_context>
@./.claude/vit/workflows/execute-plan.md
@./.claude/vit/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/phases/v1.0.1/01-infrastructure-and-ia/01-01-SUMMARY.md
@docs/.vitepress/config.mts
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create sidebar structure and all placeholder pages</name>
  <files>
    docs/.vitepress/sidebars.ts
    docs/.vitepress/config.mts
    docs/getting-started/index.md
    docs/getting-started/installation.md
    docs/getting-started/first-project.md
    docs/concepts/index.md
    docs/concepts/phases.md
    docs/concepts/milestones.md
    docs/concepts/agents.md
    docs/concepts/state.md
    docs/concepts/workflow-loop.md
    docs/commands/index.md
    docs/agents/index.md
    docs/guides/index.md
    docs/guides/build-a-project.md
    docs/guides/add-a-milestone.md
    docs/guides/debug-with-vit.md
    docs/guides/custom-agents.md
    docs/internals/index.md
    docs/internals/state-management.md
    docs/internals/github-sync.md
    docs/internals/worktrees.md
    docs/internals/hooks.md
    docs/config/index.md
    docs/config/profiles.md
    docs/config/settings.md
    docs/config/hooks.md
  </files>
  <action>
1. Create `docs/.vitepress/sidebars.ts` exporting sidebar configuration arrays for all 7 sections. Follow the sidebar hierarchy from the ARCHITECTURE.md research exactly:

   - `/getting-started/` — 3 items (Overview, Installation, First Project)
   - `/concepts/` — 6 items (Overview, Phases, Milestones, Agents, State, Workflow Loop)
   - `/commands/` — 7 groups (All Commands, Project Setup, Planning, Execution, Verification, Roadmap Management, Team & State, Configuration) with all 29 command entries. Use the exact groupings from ARCHITECTURE.md. Command page links point to `/commands/<name>` — these pages do NOT exist yet (created in Phase 03) but the sidebar structure must be committed now.
   - `/agents/` — 4 groups (All Agents, Planning Agents, Execution Agents, Verification Agents, GitHub Integration Agents) with all 16 agent entries. Agent page links point to `/agents/<name>` — these pages do NOT exist yet (created in Phase 04).
   - `/guides/` — 5 items (Overview, Build a Project, Add a Milestone, Debug with VIT, Custom Agents)
   - `/internals/` — 5 items (Overview, State Management, GitHub Sync, Worktrees, Hooks)
   - `/config/` — 4 items (Overview, Profiles, Settings, Hooks)

   CRITICAL: ALL sidebar `link` values MUST begin with `/` (leading slash) — without it, prev/next navigation breaks silently (Pitfall 10).

2. Update `docs/.vitepress/config.mts` to import sidebars from `./sidebars` and wire them into `themeConfig.sidebar`. The sidebar object maps path prefixes to sidebar arrays:
   ```typescript
   sidebar: {
     '/getting-started/': gettingStartedSidebar,
     '/concepts/': conceptsSidebar,
     '/commands/': commandsSidebar,
     '/agents/': agentsSidebar,
     '/guides/': guidesSidebar,
     '/internals/': internalsSidebar,
     '/config/': configSidebar,
   }
   ```

3. Create placeholder `.md` files for every NON-command/NON-agent page listed in the sidebar. Each placeholder must have:
   - A frontmatter `title` field
   - An H1 heading matching the sidebar text (use explicit H1, not frontmatter-derived — avoids search indexing issues from Pitfall 6)
   - A brief placeholder sentence: "This page is under construction. Content will be added in Phase XX."
   - The appropriate phase reference (Phase 02 for getting-started/concepts, Phase 05 for guides/internals/config)

   Do NOT create individual command pages (docs/commands/new-project.md etc.) or agent pages (docs/agents/vit-planner.md etc.) — those are created by stub extraction in Phases 03 and 04.

   DO create the index pages for commands/ and agents/ sections — these are hand-written overview pages, not stub-extracted.

4. The commands/index.md placeholder should include a note: "This page will contain a categorized table of all 29 commands." The agents/index.md placeholder should include: "This page will contain a grouped table of all 16 agents."

5. All internal links between placeholder pages must use extension-less format: `[link](./other-page)` not `[link](./other-page.html)` — regardless of the cleanUrls setting, this is the VitePress recommended convention (Pitfall 9).
  </action>
  <verify>
    Run `npm run docs:build` — must exit 0 with no errors (ignoreDeadLinks is temporarily true for command/agent pages that don't exist yet).
    Count placeholder files: `find docs -name "*.md" -not -path "*/\.vitepress/*" | wc -l` — should be at least 26 (1 index + 3 getting-started + 6 concepts + 1 commands index + 1 agents index + 5 guides + 5 internals + 4 config).
    Verify all sidebar links use leading slashes: `grep -c "link: '" docs/.vitepress/sidebars.ts` should equal the count of entries, and `grep "link: '[^/]" docs/.vitepress/sidebars.ts` should return nothing.
  </verify>
  <done>
    sidebars.ts exports all 7 sidebar configurations with correct groupings and leading-slash links. All non-command/non-agent placeholder pages exist. The build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the homepage with hero, features, and CTA</name>
  <files>
    docs/index.md
  </files>
  <action>
1. Replace the placeholder `docs/index.md` with a full homepage using VitePress `layout: home` frontmatter features.

2. **Hero section:**
   - name: "VIT"
   - text: "Phase-Based AI Development Framework"
   - tagline: "Orchestrate complex projects with Claude Code — from planning through verification, with full GitHub integration."
   - actions:
     - Primary CTA: "Get Started" linking to `/getting-started/`
     - Secondary CTA: "View on GitHub" linking to `https://github.com/LeVarez/vit-cc`

3. **Features section** (use VitePress `features` frontmatter array):
   Feature cards highlighting the core value propositions. Include 4-6 feature cards:
   - **Phase-Based Workflow** — Break complex projects into phases with automatic planning, execution, and verification
   - **AI Agents** — 16 specialized agents handle planning, reviewing, testing, and documentation automatically
   - **GitHub Integration** — PRs, issues, CI results, and code review — all managed through the workflow
   - **Smart State Management** — Context survives across sessions. Pick up where you left off, every time.
   - **Built-in Quality Gates** — Automated verification catches issues before they reach review
   - **Extensible** — Create custom agents tailored to your project's needs

   Each feature card has: `icon` (use emoji), `title`, and `details` (1-2 sentence description).

4. The hero and features are defined entirely in YAML frontmatter — no Markdown body content needed for the homepage layout. VitePress renders the `layout: home` page from frontmatter alone.

5. Derive copy from the existing README.md and PROJECT.md — do not invent new value claims. The homepage should reflect what VIT actually does.
  </action>
  <verify>
    Run `npm run docs:build` — must exit 0.
    Verify the index.md contains `layout: home`, a `hero` block with `actions`, and a `features` array with at least 4 entries.
  </verify>
  <done>
    The homepage renders with a hero section (title, tagline, two CTAs), at least 4 feature highlight cards, and links to the getting-started section. The build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `npm run docs:build` exits 0 with no errors
2. All 7 sidebar sections are defined in sidebars.ts with correct structure
3. Every sidebar link (for non-command/non-agent pages) points to an existing .md file
4. The homepage displays hero section with CTA and feature cards
5. All placeholder pages have an H1 heading and frontmatter title
6. No sidebar links are missing the leading `/`
7. The search bar indexes placeholder page titles (test after build by checking .vitepress/dist/ for search index)
</verification>

<success_criteria>
- The site is fully navigable — every sidebar section appears and every non-command/non-agent link resolves
- The homepage shows the value proposition and directs users to Get Started
- The sidebar structure matches the ARCHITECTURE.md specification exactly
- The build succeeds with all placeholder pages in place
</success_criteria>

<output>
After completion, create `.planning/phases/v1.0.1/01-infrastructure-and-ia/01-02-SUMMARY.md`
</output>
