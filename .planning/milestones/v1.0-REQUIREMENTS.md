# Requirements Archive: v1.0 GitHub Sync & Agents Extension

**Archived:** 2026-03-18
**Status:** ✅ SHIPPED

This is the archived requirements specification for v1.0.
For current requirements, see `.planning/REQUIREMENTS.md` (created for next milestone).

---

## v1 Requirements

### PR Lifecycle

- [x] **PRL-01**: `execute-phase` creates a draft PR targeting `milestone/vX.Y` at the start of execution (not at plan-phase)
- [x] **PRL-02**: Draft PR creation is idempotent — running `execute-phase` twice on the same phase does NOT create a duplicate PR
- [x] **PRL-03**: STATE.md has a `PR` column in the GitHub Issue Mapping table; draft PR number is written as `pr#N` when created
- [x] **PRL-04**: `verify-work` promotes draft PR to ready-for-review (`gh pr ready`) on Route A only (all checks pass AND more phases remain)
- [x] **PRL-05**: STATE.md PR column is updated from `pr#N` to `pr#N(ready)` when `verify-work` promotes the PR
- [x] **PRL-06**: All PR operations (creation, promotion) degrade gracefully when `gh` CLI is unavailable — log one line and continue

### PR Reviewer

- [x] **PRR-01**: `vit-pr-reviewer` agent is spawned automatically by `verify-work` after a draft PR is promoted (Route A only) — no manual invocation needed
- [x] **PRR-02**: The review comment visibly categorizes findings into `block-merge`, `should-fix`, and `nit` tiers — a reader can immediately see which findings block merging
- [x] **PRR-03**: The agent posts inline diff comments on specific changed lines (via `gh api` REST endpoint), not just a body-only comment

### Documentation & Changelog

- [x] **DOC-01**: `vit-doc-updater` agent spawns after all phase waves complete in `execute-phase`
- [x] **DOC-02**: Agent autonomously reads phase SUMMARY.md and PLAN.md to determine which docs and sections need updating — no manual configuration required
- [x] **DOC-03**: Agent updates only targeted sections of affected documentation files (section-scoped, preserves all other content verbatim)
- [x] **DOC-04**: Agent appends to `[Unreleased]` section in CHANGELOG after phase completes
- [x] **DOC-05**: `vit-changelog-writer` agent spawns inside `complete-milestone` before the archive step
- [x] **DOC-06**: Changelog writer updates ALL project documentation (README, docs/, API docs) and generates a versioned CHANGELOG entry — reads all phase SUMMARY.md files to understand the full scope of delivered work

## Traceability

| Requirement | Phase | Status |
|------------|-------|--------|
| PRL-01 | Phase 1 | Complete |
| PRL-02 | Phase 1 | Complete |
| PRL-03 | Phase 1 | Complete |
| PRL-04 | Phase 1 | Complete |
| PRL-05 | Phase 1 | Complete |
| PRL-06 | Phase 1 | Complete |
| PRR-01 | Phase 2 | Complete |
| PRR-02 | Phase 2 | Complete |
| PRR-03 | Phase 2 | Complete |
| DOC-01 | Phase 3 | Complete |
| DOC-02 | Phase 3 | Complete |
| DOC-03 | Phase 3 | Complete |
| DOC-04 | Phase 3 | Complete |
| DOC-05 | Phase 3 | Complete |
| DOC-06 | Phase 3 | Complete |

**Coverage:** 15/15 v1 requirements shipped ✓

---

## Milestone Summary

**Shipped:** 15 of 15 v1 requirements
**Adjusted:** None — all requirements implemented as specified
**Dropped:** None

**Notable implementation notes:**
- PRR-03 (inline diff comments): capped at 5 inline comments to avoid GitHub API 422 errors; `block-merge` findings prioritized first
- DOC-01 spawn point: step 10.6 in execute-phase (after commit, before push) — not per-task, per-phase
- DOC-05 spawn point: step 3.5 in complete-milestone (after extract_accomplishments, before archive_milestone)

---
*Archived: 2026-03-18 as part of v1.0 milestone completion*
